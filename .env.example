# Copy to .env and fill in. .env is git-ignored.
# Used by local setup scripts; never read by deployed code.

GCP_PROJECT_ID=
VERTEX_LOCATION=us-central1
VERTEX_MODEL=gemini-2.5-flash-lite
USER_TIMEZONE=Asia/Jerusalem
